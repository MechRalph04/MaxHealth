name: MaxHealth
author: Ralph
description: It will heal you by using /max
version: 1.0.3
main: HealCOMMANDS\HealCommand\Cmd
api: [3.0.0-ALPHA4]

commands:
  mht:
    description: Heals you. Hope you heal and get very well
    usage: "Only OP's can use this and in-game Main Usage: /max [in-game only] /max <Player Name>"

permissions:
  heal.cmd.max:
    default: op
    description: "Heal your self by /max (BEWARE!)"
  heal.cmd.max.other:
    default: op
    
    
